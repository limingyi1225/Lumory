# Superreview Report — 2026-04-25 11:31

## 改动概览
- 78 文件 / +2282 / -2848 (working tree vs HEAD `305e8ea`)
- 已应用的修复 4 处 (本次 review 期间):
  1. OpenAIService.swift `ParserError.invalidEvent(String)` → `(byteCount: Int)`,堵 raw payload 经 `localizedDescription` 进失败 UI 的泄漏路径
  2. NetworkRetryHelper.swift Retry-After 现在覆盖 429 / 502 / 503 / 504
  3. HomeView.swift `SendSnapshot` 新增 `audioDuration` 字段,失败回滚不再显示 `0:00`
  4. (Codex P1 已合并到 #1)

## Reviewer 视角
| 视角 | subagent | 状态 |
|---|---|---|
| correctness | code-reviewer (Opus) | ✅ 完成 — 17 条 finding |
| concurrency | general-purpose (Opus) | ✅ 完成 — 8 条 finding |
| coredata-migration | coredata-migration-reviewer (Opus) | ✅ 完成 — 0 P0,5 P1 全 advisory |
| convention/gotcha | code-reviewer (Opus) | ✅ 完成 — 全部 18 项 clean,1 条 P3 advisory |
| codex (跨模型) | codex:codex-rescue | ✅ 完成 — 1 P1 + 3 P2 |
| security-auditor / backend-security-coder | (本机插件未提供,未跑) | ⚠️ skipped |
| performance-engineer / architect-review / test-automator / api-design-principles | (本机插件未提供,未跑) | ⚠️ skipped |

> 缺失视角注:本机 `~/.claude` 插件集只暴露了上面"完成"那一列对应的 subagent 类型。对应的 backend / iOS security 与 perf / arch / test / API contract 视角没跑,本报告里这些 dimension 的覆盖**不完整**。如要补,改成 `general-purpose` + 专项 prompt 重跑。

---

## P0 — 必修 (commit/push 前)

**无 P0**。所有 reviewer 都没报真正的 P0(数据丢失 / crash / 鉴权破洞 / fail-open 等)。

---

## P1 — 已修

### 1. SSE ParserError 泄漏 raw payload 到失败 UI
- **来源**:codex (P1) + correctness reviewer (P2 旁证)
- **位置**:[Chronote/Services/OpenAIService.swift:626](Chronote/Services/OpenAIService.swift:626)
- **问题**:`ParserError.invalidEvent(String)` 把 SSE payload 头 120 字符塞进 `LocalizedError.errorDescription`。decode 失败 → throw → `error.localizedDescription` → InsightsEngine.swift:319 / NarrativeReader.swift:179 把它当 failure 文案直接渲染给用户。raw payload 可能含日记内容碎片或 AI 响应碎片,不应进 UI。Log.error 那一路因为 `Log.swift:70` 是 `.private` OSLog,console 不会泄漏,但 UI 路径完全暴露。
- **核对**:已 grep + Read 确认 (`OpenAIService.swift:629,678,686`,`InsightsEngine.swift:319`,`NarrativeReader.swift:179`,`Log.swift:64-72`)
- **已修复**:case 改成 `invalidEvent(byteCount: Int)`,`errorDescription` 改成 "SSE stream contained an invalid event (N bytes)."。test `ChronoteTests.swift:1003` 的 `contains("invalid event")` 仍通过。`upstreamError(String)` 保留 — 那是后端 envelope 的控制平面错误描述,不是流内容。

### 2. NetworkRetryHelper 的 `Retry-After` 只对 429 生效
- **来源**:correctness reviewer (Opus)
- **位置**:[Chronote/Services/NetworkRetryHelper.swift:111](Chronote/Services/NetworkRetryHelper.swift:111)
- **问题**:`errorForStatus(_:retryAfter:)` 在 OpenAIService.swift:269-297 上对**所有** status 都把 header 塞进 userInfo,但 `retryAfterDelay(from:)` 只在 `code == 429` 时才读。结果:后端或上游 OpenAI 在 502 / 503 / 504 上附 `Retry-After: 30` 时,客户端会无视 header,落回 1-2-4s 指数回退,在维护窗口期反复撞墙。
- **核对**:`grep "errorForStatus"` 确认所有 throw 调用点都传 header(OpenAIService.swift:220, 370, 407, 504, 1160, 1317);read 确认 errorForStatus 对全 status 都 attach
- **已修复**:guard 改为 `[429, 502, 503, 504].contains(nsError.code)`

### 3. HomeView 失败回滚的 Recording 时长丢失
- **来源**:correctness reviewer (Opus)
- **位置**:[Chronote/Views/HomeView.swift:957](Chronote/Views/HomeView.swift:957)
- **问题**:`SendSnapshot` 只 capture audio 文件名,没 capture duration。失败回滚时硬写 `Recording(... duration: 0)`,UI 显示 0:00 即便文件还在、能播。原始 Recording 在 line 1407 是用 `recorder.duration` 创建的。
- **核对**:Read 确认 SendSnapshot struct(原 line 850-856),snapshot 创建点(line 877-884),失败回滚点(line 956-958)
- **已修复**:`SendSnapshot` 加 `audioDuration: TimeInterval`,捕获 `recordingVM.audioRecordings.first?.duration ?? 0`,失败回滚改用此值

---

## P1 — 已核对后否决 / 降级

### A. SSEParser 新行为(throw on decode failure)是回归
- **来源**:correctness reviewer (Opus)
- **核对**:Read `git show HEAD:Chronote/Services/OpenAIService.swift` (line 882-908) 确认旧行为是 `try? decoder.decode + if let decoded ...`,silent skip。新行为(line 680-689)是 throw `ParserError.invalidEvent`,若 decode 不成且非 OpenAI error envelope。
- **裁决**:**否决,这是有意设计**。新代码增加了 `ChronoteTests.swift:995-1007 invalidJSONThrowsInvalidEvent` 把"throw"行为 pin 死。且实际 OpenAI chat-completions stream 里:
  - chunk 帧 → 解码为 `OpenAIStreamResponse` 成功
  - 最后 usage 帧 → `choices: []` 空数组,仍然解码成功
  - 控制平面错误 → 解码为 `OpenAIStreamErrorEnvelope`,throw `upstreamError`(走 UI 失败 banner,期望行为)
  - 真"未知 shape"在当前后端架构里没有现实触发源
- **风险评估**:理论 P3。如果后端将来切到不同上游(Anthropic / 自家 fallback)且 emit 新 shape,需要回补 silent-skip。现在保留 strict 行为。

### B. `kCFErrorDomainCFNetwork` retry 分类被删
- **来源**:correctness reviewer (Opus,P1)
- **核对**:`git diff HEAD -- NetworkRetryHelper.swift` 确认 line 90-94 整个分支被删。剩下的 `isRetryableError` 覆盖 NSURLErrorDomain 14 种 code + OpenAIService 5xx + 429。
- **裁决**:**降级到 P2,不修复**。iOS URLSession 99% 把底层 CFNetwork 错误翻译成 `NSURLErrorDomain` 编码;直接以 `kCFErrorDomainCFNetwork` 露面的几乎只有 background session 等边缘场景,日记 App 路径不沾。是防御性损失,不是用户可见 regression。

### C. EmbeddingBackfill / ThemeBackfill 在 `fetchMissingObjectIDs` 之后缺 `Task.isCancelled` 早退
- **来源**:concurrency reviewer (Opus,P1)
- **核对**:Read `EmbeddingBackfillService.swift:103-127`,`publish` 内部 line 204-205 已经 `guard runningRunID == runID` —— cancel() 把 runID 清零,所有 stale publish 自动丢弃。
- **裁决**:**降级到 P3,不修复**。代价仅一次 SQL 扫 + 1-2 个被丢弃的 publish。无数据正确性影响,无 UI 闪烁(被 runID guard 拦了)。

### D. CloudKitSyncMonitor 删了 1.5s `.synced` 兜底,UI 卡 `.syncing`
- **来源**:correctness reviewer (Opus,P2)
- **核对**:Read 确认 `git diff` 删了 asyncAfter 块。
- **裁决**:**P2 待用户决定**。如果用户体感"sync 状态卡住",再加回来;否则保持当前状态(等真实 CK event)。

### E. `CoreDataImportService` 改成单次 batch save,失败粒度变粗
- **来源**:correctness + coredata reviewer (P2)
- **核对**:Read `CoreDataImportService.swift:107-121`
- **裁决**:**P2 设计选择,不算 bug**。日记导入 atomic 是合理 UX。建议 import view 在 `result.failed == result.succeeded == N` 时给清晰提示。

### F. `OpenAIService.streamReportEvents` 双层 catch 可能重复发 `.truncated`
- **来源**:correctness reviewer (Opus,P2)
- **核对**:未深入核对 (代码路径复杂)。
- **裁决**:**P2 待确认**。实际 race 触发条件极窄(inner emit `.truncated` 后 closure 抛错)。建议下次 OpenAIService 改动一并审。

### G. `awakeFromInsert` 仅 local-insert 触发,CK 拉来的对象不走
- **来源**:coredata-migration-reviewer
- **核对**:Read 确认 `id: UUID?` 仍 optional,显式 assignment 在 `DiaryEntryRow.swift:269` / `CoreDataImportService.swift:92` / `UITestSampleData.swift:151` / `DiaryEntry+CoreDataProperties.swift:85` 都还在
- **裁决**:**advisory,不是 bug**。schema 没改,CK 兼容。建议在 `awakeFromInsert` 加 comment 说明它仅 local-insert,显式 assignment 是 CK pull 的安全网。

### H. `CoreDataImport fingerprint` 用 Double `timeIntervalSinceReferenceDate` 容易 round-trip 失配
- **来源**:coredata-migration-reviewer (P1)
- **核对**:Read `CoreDataImportService.swift:138-140`
- **裁决**:**P2,可选优化**。建议改 `Int(date.timeIntervalSince1970)` 量化到秒。当前同进程 import 内 fingerprint 一致;CloudKit pull 进来的 Date 序列化精度漂移会导致重复检测失效。下次有人改 import 时一并改。

### I. `NSPersistentStoreRemoteChange` observer 删了 `refreshAllObjects`
- **来源**:coredata-migration-reviewer (P1) + convention reviewer (P3)
- **核对**:Read `PersistenceController.swift:144-153`。`automaticallyMergesChangesFromParent = true` 在 line 140
- **裁决**:**有意改进,不是 regression**。删除是为了不丢 viewContext 的未保存输入。CK history-driven merge 会重新 fault 变更对象,UI 反应正常。如果将来报告"另一台设备改了内容,iPhone 显示 stale",再改回 `mergeChanges(fromRemoteContextSave:into:)`(不要用 `refreshAllObjects`)。

---

## P2 — 主 agent 暂未处理(交给用户)

### 1. server SSE 缺"upstream 无 [DONE] 早结束"的回归测试
- **来源**:codex (P2)
- **位置**:[server/index.js:294](server/index.js:294) 新增了 sawDone tail-scan,但 `server/index.test.js` 没覆盖
- **修复方向**:加 `node --test` case mock 上游不发 [DONE] 直接结束,断言后端 `res.destroy()` 而非写 `data: [DONE]`

### 2. InsightsView dailyCellsCache 在 CloudKit/本地变更后会保留 stale
- **来源**:codex (P2)
- **位置**:[Chronote/Views/Insights/InsightsView.swift:261](Chronote/Views/Insights/InsightsView.swift:261)
- **修复方向**:cache key 加 store 变更 token 或在 `NSPersistentStoreRemoteChange` / `NSManagedObjectContextDidSave` 时失效

### 3. CoreDataImport 重复检测每次扫 viewContext 全表
- **来源**:codex (P2,但其"main context"断言主 agent 未深入核对)
- **位置**:[Chronote/Services/CoreDataImportService.swift:127](Chronote/Services/CoreDataImportService.swift:127)
- **修复方向**:挪到 `performBackgroundTask`,按 import 候选 date 范围窄化预测

### 4. `AskPastView` auto-scroll 节流可能漏最后一段
- **来源**:correctness reviewer
- **位置**:[Chronote/Views/Insights/AskPastView.swift:195-201](Chronote/Views/Insights/AskPastView.swift:195)
- **修复方向**:在 `isStreaming` flip 回 false 时再补一次 scrollTo

### 5. AudioRecorder session mode 改成 `.measurement` + `.mixWithOthers`
- **来源**:correctness reviewer
- **位置**:[Chronote/Services/AudioRecorder.swift:84-89](Chronote/Services/AudioRecorder.swift:84)
- **修复方向**:`.measurement` 关 AGC / 降噪 / 回声消除,会影响 SFSpeechRecognizer 转写质量。如目的是允许背景音乐播放,只加 `.mixWithOthers`,模式留 `.default` / `.spokenAudio`。建议跑一次实测对比转写 WER。

### 6. EmbeddingBackfill 的 `fetchBatchSize=200` 在 `.managedObjectIDResultType` 下是 no-op
- **来源**:correctness reviewer
- **位置**:[Chronote/Services/EmbeddingBackfillService.swift:160](Chronote/Services/EmbeddingBackfillService.swift:160)
- **修复方向**:删除该行,或换成 `.managedObjectResultType` + `propertiesToFetch = []`

### 7. `PromptSuggestionEngine.refreshIfNeeded` 总付 buildContext 代价
- **来源**:correctness reviewer
- **位置**:[Chronote/Services/PromptSuggestionEngine.swift:113-118](Chronote/Services/PromptSuggestionEngine.swift:113)
- **修复方向**:freshness check 移到 `await buildContext()` 之前

### 8. `streamReportEvents` retry 闭包 capture mutable var
- **来源**:concurrency reviewer
- **位置**:[Chronote/Services/OpenAIService.swift:196-249](Chronote/Services/OpenAIService.swift:196)
- **修复方向**:future-proof under strict concurrency。当前 single-Task driver 下不竞态,可不动

### 9. `NarrativeReader.streamTask` 自然结束后未 nil
- **来源**:concurrency reviewer
- **位置**:[Chronote/Views/Insights/Components/NarrativeReader.swift:159-189](Chronote/Views/Insights/Components/NarrativeReader.swift:159)
- **修复方向**:loop 结束后 `streamTask = nil`,防止重启 silently no-op

### 10. EmbeddingBackfill / ThemeBackfill 默认 ai 由空 stub 改成 `OpenAIService.shared`
- **来源**:coredata-migration reviewer
- **位置**:`EmbeddingBackfillService.swift:51`,`ThemeBackfillService.swift:50`
- **核对**:`grep`-过 callers 只有 `.shared` 单例,没有外部 `init(ai:)` 显式传入,所以行为一致。**flag for awareness only**

---

## Lumory 已知踩坑清单核对结果

按 skill 自带的 18 项清单核对(convention reviewer 已跑全):

- ✅ 1-17 全部 clean,无回归
- 18 (新构建脚本):全部加 `set -euo pipefail` 是 hardening,无 PIPESTATUS 误用

附加自查:
- ✅ ATS 例外:Lumory-Info.plist 无任何 NSAppTransportSecurity / NSAllowsArbitraryLoads
- ✅ 后端 fail-closed:`server/index.js:42` (OPENAI_API_KEY) + `:54` (APP_SHARED_SECRET) `process.exit(1)` 保留
- ✅ 后端 SSE 错误路径 `res.destroy(error)` 保留(`:317`),新增 `:305` "ended without [DONE]" 也走 `res.destroy`,无 `data: [DONE]` 误报成功
- ✅ Embedding V1 magic + legacy fallback 解码路径完整
- ✅ DataMigrationService 仍在 `Task.detached(.userInitiated)` 不阻塞 init
- ✅ Embedding/Theme backfill 仍用户主动触发,无 auto

---

## 建议下一步

1. **可以 commit 整个 working tree**(78 + 4 文件)。已修 P1 全部落地,build 通过,无 P0 阻塞
2. 用户决定 P2 #1 / #2 / #5(SSE 测试 / 缓存失效 / AudioSession mode)是否本轮一并处理
3. 缺失的 4 个 reviewer 视角(security / perf / arch / API contract)如果想补,改 `general-purpose` + 专项 prompt 重跑一轮
